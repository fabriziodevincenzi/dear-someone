# Previous home

The former `/launch/` home has been archived here and is intentionally outside
Astro's `src/pages` directory, so it is not published as a route.

The source remains available in Git history under `src/pages/launch/index.astro`.
