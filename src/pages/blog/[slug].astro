---
import { blogArticles } from '../../lib/blog';

export function getStaticPaths() {
  return blogArticles.map((article) => ({
    params: { slug: article.slug },
  }));
}

return Astro.redirect(`/journal/${Astro.params.slug}/`);
---
